/*package com.esp.jpaRepo;

import com.esp.models.Esp;
import org.springframework.data.jdbc.repository.query.Query;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

//@Repository
public interface EspJpaRepo extends JpaRepository<Esp, Long> {
    //@Query(value = "INSERT into Esp (id, name, date, time) VALUES (?1, ?2, ?3, ?4)", nativequery=true);
    //@Query(value = "INSERT into Esp (id, name) VALUES (?1, ?2)")
    Esp createEsp(String name);
}*/
